.zig-cache/
zig-out/
zig-pkg/
*.o
*.obj
*.dll
# wasm build artifacts (e.g. island_GlDemo_test.wasm from zig build test)
*.wasm
# …except the vendored MS-redistributable WebView2 loader: the win-native
# gate, both desktop templates, and the scaffold embed need it present in
# a fresh clone (the build is offline — no NuGet fetch at build time).
!src/desktop/win_native/include/WebView2Loader.dll
!src/desktop/win_native/include/arm64/WebView2Loader.dll
.DS_Store

# Local-dev superpowers artifacts (specs/plans) — never committed
docs/superpowers/

# Git worktrees
.worktrees/

# Local Claude Code project instructions (do not track)
CLAUDE.md
