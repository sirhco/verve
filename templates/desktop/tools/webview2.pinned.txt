# Pinned WebView2 SDK release. `fetch_webview2.{sh,ps1}` downloads
# this exact NuGet version into `third_party/webview2/` so all builds
# link against the same loader binary. Bump the version line below
# and refresh the sha512 next to it before pushing — the scripts
# will refuse to overwrite a populated SDK directory unless
# `--force` is passed.
version=1.0.2849.39
# Base64-encoded SHA-512 of microsoft.web.webview2.<version>.nupkg.
# To refresh after bumping `version=`:
#
#   curl -fsSL https://www.nuget.org/api/v2/package/Microsoft.Web.WebView2/<version> \
#     | openssl dgst -sha512 -binary | base64 | tr -d '\n'
#
# (NuGet's flatcontainer publishes a `.nupkg.sha512` sibling for
# recent uploads, but not for older versions — computing locally is
# the reliable path. Both endpoints serve byte-identical packages.)
# Leave blank to skip verification; set to the base64 hash to enforce.
sha512=1yhNeWQKXtPMVerjHkfFMTTu0xlaf04ph+sDlrvWV8SZycfDFYEwF7F5vtRCiu+UKrzamXEjBej0RHhJYVaZmg==
